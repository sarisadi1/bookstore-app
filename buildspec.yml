version: 0.2
phases:
  install:
    runtime-versions:
      java: 21
  build:
    commands:
      - echo Packaging JAR file...
      - mvn clean package
  post_build:
    commands:
      - echo Build complete.
artifacts:
  files:
    - target/BookStore.jar
  discard-paths: yes